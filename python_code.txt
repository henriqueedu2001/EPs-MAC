/*
  pilha = []
  vetor = [1,2,3,4]
  qtd = 0
  tam_seq = 4
  pilha.append(0)
  qtd += 1
  while 1:
    # último elemento extrapola => remover e incrementar o anterior
    # caso contrário:
      # pilha não ok => incrementar último
      # caso contrário (pilha ok)
        # pilha incompleta => add(0)
        # pilha completa => print e incrementar último
        
    if pilha[len(pilha) - 1] > len(vetor) - 1:
      # último elemento extrapola => remover e incrementar o anterior
      pilha.pop()
      if len(pilha) == 0:
        break
      else:
        pilha[len(pilha) - 1] += 1
    else:
      if ok(vetor, pilha) == 0:
        # pilha não ok => incrementar último
        pilha[len(pilha) - 1] += 1
      else:
        if len(pilha) < tam_seq:
          pilha.append(0)
        else:
          print_seq(vetor, pilha)
          pilha[len(pilha) - 1] += 1
  print("fim")
    
    
def print_seq(vetor, pilha):
  seq = []
  for i in range (0, len(pilha)):
    seq.append(vetor[pilha[i]])
  print(seq)
      
def ok(vetor, pilha):
    i = 0
    j = 0
    for i in range(0, len(pilha)):
        for j in range(i + 1, len(pilha)):
            if vetor[pilha[i]] == vetor[pilha[j]]:
                return 0
    return 1

*/